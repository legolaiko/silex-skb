{% form_theme formLogin 'form-bootstrap-theme.twig' %}
{% extends 'layout.twig' %}

{% block content %}
    <div class="row user-forms">
        <div class="col-md-5">
            <ul class="nav nav-tabs">
                <li class="active"><a href="{{ url('user_login') }}">{{ 'Sign in'|trans }}</a></li>
                <li><a href="{{ url('user_register') }}">{{ 'Sign up'|trans }}</a></li>
            </ul>
            {{ form(formLogin) }}
        </div>
    </div>
{% endblock %}